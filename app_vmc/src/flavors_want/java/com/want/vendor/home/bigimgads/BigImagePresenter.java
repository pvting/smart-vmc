package com.want.vendor.home.bigimgads;

import com.want.base.sdk.framework.app.mvp.AbsPresenter;

/**
 * Presenter stub.
 */
public class BigImagePresenter extends AbsPresenter implements BigImageContract.Presenter {

    public BigImagePresenter(BigImageContract.View view) {
        super(view);
    }

    @Override
    @SuppressWarnings("unchecked")
    protected BigImageContract.View getView() {
        return super.getView();
    }

    // TODO

}