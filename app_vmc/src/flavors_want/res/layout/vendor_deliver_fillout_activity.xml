<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <android.support.percent.PercentRelativeLayout
        android:id="@id/vendor_home_style_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:id="@+id/vendor_deliver_show"
            android:layout_width="match_parent"
            app:layout_heightPercent="90.7%"/>

        <FrameLayout
            android:id="@+id/vendor_deliver_back"
            android:layout_below="@+id/vendor_deliver_show"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            app:layout_heightPercent="9.3%" />

    </android.support.percent.PercentRelativeLayout>
</layout>
